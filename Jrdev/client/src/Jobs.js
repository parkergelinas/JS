import React from "react";
import Typography from "@mui/material/Typography";

import Job from "./Job";

export default function Jobs() {
  return (
    <div className="job-list">
      <Typography variant="h1">Entry Level Jobs</Typography>
    </div>
  );
}
